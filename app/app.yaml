command:
  - uvicorn
  - main:app
  - --host
  - 0.0.0.0
  - --port
  - 8000

env:
  - name: SUPERVISOR_ENDPOINT
    value: "mas-5d3d2f6b-endpoint"

resources:
  - name: supervisor-endpoint
    serving_endpoint:
      name: "mas-5d3d2f6b-endpoint"
      permission: CAN_QUERY
